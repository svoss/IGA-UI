version: 1
cron:
 - name: "fv-work"          # required - unique across all entries in this file
   url: "/FV/work"              # required - does not need to be unique
   schedule: "* */1 * * *"
 - name: "fv-analytics"          # required - unique across all entries in this file
   url: "/FV/analytics"              # required - does not need to be unique
   schedule: "* * * * *"
 - name: "tuxx-work"          # required - unique across all entries in this file
   url: "/tuxx/work"              # required - does not need to be unique
   schedule: "* */1 * * *"
 - name: "tuxx-analytics"          # required - unique across all entries in this file
   url: "/tuxx/analytics"              # required - does not need to be unique
   schedule: "* * * * *"